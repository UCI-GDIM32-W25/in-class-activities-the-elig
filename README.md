# GDIM32 In Class Activity Devlogs
## Week 1
1. Double check that itch.io and GitHub have properly updated when submitting any assignment. For example, open your itch.io link in an incognito tab to make sure it works.
2. a. 10
   b. 2
   c. It would print "hello world" to the console every frame the program is running
   d. MonoBehaviour
   e. It would print "x = 10" to console at program start up
   f. Argument. To send a variable into a method's parameter to allow more flexible use of methods (rather than hard coding every function)
   g/h. You cannot directly call the class name Transform, as it isn't static. You have to call the instance variable of type Transform (_playerTransform)
